server:
  port: 20400

spring:
  application:
    name: mall-log
  profiles:
    active: '@spring.active@'
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
        watch-delay: 1000

